# netlify.toml
# Configuración de build y redirecciones para Netlify

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_API_BASE"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
